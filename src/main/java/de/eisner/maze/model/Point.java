package de.eisner.maze.model;

/**
 * Represents a coordinate within the maze grid.
 * @param row The row index (y-axis).
 * @param col The column index (x-axis).
 */
public record Point(int row, int col) {
}
